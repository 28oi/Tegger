export interface Participant {
    name: string;
  }
  
  export interface Song {
    title: string;
  }
  
  export interface Schedule {
    date: string;
    time: string;
    location: string;
  }
  